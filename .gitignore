venv/
.env
__pycache__/
*.pyc
.faiss/
outputs/

# runs/ is NOT ignored — run artifacts ARE committed so the thesis writer
#       (LLM or human) can read every per-run JSON / report / event log.
# (line intentionally left commented in the original .gitignore for reference)

.pytest_cache/
*.db
.DS_Store
CLAUDE.md

# Claude Code local state (settings, scheduled tasks)
.claude/

# IDE-local preferences
.vscode/
